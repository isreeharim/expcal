## 2026-08-09 - Missing ARIA Labels on Icon-only Dropdown Triggers\n**Learning:** The project's `DropdownMenuTrigger` components (from shadcn/ui) frequently use icon-only content (like `MoreVertical`) without providing accessible names, making them unreadable to screen readers.\n**Action:** Always ensure icon-only interactive elements in this app's components include a descriptive `aria-label` attribute.
